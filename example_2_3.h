/* 파일입출력, 콘솔입출력, 문자입출력 등등
 * 다양한 입력 및 출력에 관한 기능이 정의된
 * stdio.h 헤더 파일을 현재 작성하는 소스 파일에
 * 추가하는 선행처리 부분
 */
#include <stdio.h>
#define ADD(x, y) ((x)+(y)) // 더하기 매크로 함수 ADD를 정의, x와 y를 더함
#define SUB(x, y) ((x)-(y)) // 빼기 매크로 함수 SUB를 정의, x와 y를 뺌